name = "randb-site-valuator"
compatibility_date = "2025-09-27"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = "./dist"

# SPA routing - serve index.html for all unmatched routes
[assets.serving]
not_found_handling = "single-page-application"
